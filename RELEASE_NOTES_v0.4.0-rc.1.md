# Minerva Ranger v0.4.0-rc.1

这是 V0.4 的首个 Windows x64 预发布候选，用于真实历史存档、备份恢复、覆盖升级和卸载交互的最终人工验收。它不是稳定版 `v0.4.0`。

## 本次更新

- 将巡林官成长、金币、收藏、共享地图、累计森林和可移栽树木整合为跨身份共享的全局档案。
- Windows Electron 桌面版改用主进程独占的 SQLite 权威存档，支持事务修订、模式迁移、失败回滚和结构化错误。
- 支持旧 localStorage 一次性迁移并保留原始副本；JSON 导入前自动备份，并提供手动备份与受保护恢复。
- 新增引导式 Windows 安装器，可选择安装范围和程序安装目录。
- 卸载器提供两个互斥选项：默认仅删除应用并保留本地数据，或二次确认后删除应用及全部本地数据。

## 下载与校验

- 安装包：`MinervaRanger-Setup-0.4.0-rc.1.exe`
- 平台：Windows x64
- SHA-256：`641C4E2EED52C41C35A0B0236506E68108A0BEA2973AF18F16968DC5167BCFF9`

Windows 可能因为安装包尚未代码签名而显示 SmartScreen 信誉提示。

## 已验证

- `npm run check`
- `npm test`（50/50）
- Electron SQLite 探针、开发态启动和打包态启动
- NSIS 自定义目录安装、覆盖安装、默认保留数据卸载、显式完整删除和外部 JSON 保留
- 卸载器两个互斥单选项，以及危险操作确认默认选择“否”

## 发布候选仍需人工确认

- 使用真实 V0.3 历史用户目录核对迁移结果。
- 在打包应用内完成“创建备份 → 修改数据 → 恢复备份”。
- 使用旧版安装包执行真实跨版本覆盖升级。
- 在正式用户身份下分别走完两种卸载路径。

完成这些验收并处理发现的问题后，再发布稳定版 `v0.4.0`。
