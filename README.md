# 🦉 密涅瓦的巡林官 (Minerva Ranger)

> 当 **Forest（专注森林）** 遇上 **Melvor Idle（梅尔沃放置）**，把技能、行动和日志沉淀成一片可生长的生活森林。

![Version](https://img.shields.io/badge/version-0.1.0-green)
![License](https://img.shields.io/badge/license-MIT-blue)
![Vue](https://img.shields.io/badge/Vue-3.x-green)

## 📖 项目简介

**密涅瓦的巡林官**（Minerva Ranger）是一款带有轻度挂机、行动成长和日志沉淀体验的专注工具。

它不是简单记录“你专注了多久”，而是把专注行为映射到一个持续成长的行动系统里：你选择行动、种植树木、积累经验、沉淀日志，并通过技能、行动、手记和系统记录来生长自己的生活森林。

当前仓库已经完成 **0.1.0** 的本地单机核心目标，后续版本将在此基础上继续完善长期使用体验。正式的 **1.0.0** 版本会在核心体验与长期使用流程完善后发布。

## 🧭 当前状态

- 当前版本：`0.1.0`，本地单机核心目标已完成。
- 当前重点：成熟的 localStorage 多存档、行动驱动的专注成长、森林巡视和日志记录。
- 数据路线：先完成可靠的 localStorage 版本，再实施到 SQLite 的迁移。
- 兼容边界：`0.1.0` 是首个存档基线，发布前没有需要迁移的用户存档，因此不兼容此前实验阶段的存储键和存档结构。从 `0.1.0` 开始，后续存档结构调整需要提供迁移方案。
- 运行边界：当前分支只实现本地单机链路，不包含账号、云同步或云服务器运行链路。
- Electron 计划：现有基础入口和配置暂时保留，但不属于 `0.1` 至 `0.3` 的开发与验收范围；完成 `0.3` 后再继续桌面运行和安装包工作。
- 发布计划：`1.0.0` 会作为第一个正式发布版本。

## ✨ 主要功能

### 1. 行动驱动的专注成长
- 每个 `Action` 都有独立等级、经验、树木和累计时长。
- 专注收益会直接结算到行动，而不是停留在简单标签统计。

### 2. Skill / Action 管理
- 支持创建 `Skill` 和 `Action`。
- 支持按技能展开浏览、拖拽排序和重新归类行动。

### 3. 多存档位
- 支持多个独立存档位。
- 支持新建、进入、重命名、删除、导入和导出单个存档。

### 4. 巡林官手记
- 内含 `植树日志`、`系统日志` 和 `巡林日志（预留）` 三个分区。
- 植树日志可编辑，系统日志独立展示，并支持按 `Skill -> Action` 筛选。

### 5. 系统日志与行动操作
- 行动合并、行动删除都会自动生成系统日志。
- 合并或删除时可填写说明，作为日志中的操作备注保留。

### 6. 巡林与地图
- 可从地图直接进入对应技能的森林巡视页。
- 巡视页支持按行动列表浏览，并进入单个行动查看森林详情。

### 7. 商店与树种解锁
- 支持使用金币解锁不同树种。
- 已提供基础商店和昼夜切换。

### 8. 本地存档
- 本地多存档是当前版本的唯一正式数据源。
- 每个存档保留上一份 localStorage 备份；主副本损坏时会尝试恢复。
- 存档索引损坏时会根据实际存档槽重建。
- 支持导入和导出，作为用户可控的备份与迁移入口。

## 🧱 技术栈

- `Vue 3`
- `Pinia`
- `Vite`
- `Tailwind CSS`
- `Electron`（仅保留基础入口与配置，`0.3` 之后继续开发）

仓库中也已经包含 `Capacitor` 相关依赖，为后续移动端扩展留了空间。

## 🚀 本地开发

### 环境要求

- `Node.js` `^20.19.0 || >=22.12.0`

### 安装依赖

```bash
npm install
```

### 启动 Web 开发环境

```bash
npm run dev
```

身份档案保存在当前设备的 localStorage 中，不依赖账号、网络或外部服务。建议定期导出 JSON 备份。

### 构建前端资源

```bash
npm run build
```

### Electron 状态

Electron 入口和打包配置暂时保留，但当前不进行桌面启动、安装包构建或平台验收。相关开发将在 `0.3` 完成后继续。

## 🗂️ 当前主要界面

- `存档选择`：进入、新建、导入导出存档
- `商店`：购买和解锁树种
- `密涅瓦地图`：按技能进入森林巡视
- `森林巡视`：以行动列表浏览某个技能下的行动
- `行动仪表盘`：进行专注、种树和收获
- `巡林官手记`：查看与编辑植树日志、查看系统日志

## 🛣️ 接下来计划

- [x] 完成 `0.1.0` 本地单机核心流程验收
- [ ] 整理 `1.0.0` 正式版发布清单
- [ ] 将成熟的 localStorage 存档迁移到 SQLite
- [ ] 更完整的巡林日志工作流
- [ ] 更丰富的森林 / 场景视觉系统
- [ ] 成就系统
- [ ] `0.3` 完成后继续 Electron 桌面运行、打包与平台验收
